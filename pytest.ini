# pytest.ini (project root)
[pytest]
# where to look for tests
testpaths = test
# which files to consider tests
python_files = test_*.py
# automatically measure coverage on these packages and show missing lines
addopts = 
    --cov=controllers 
    --cov=data 
    --cov=models 
    --cov-report=term-missing
